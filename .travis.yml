language: ruby
rvm:
  - 2.0.0
env:
  - DB=sqlite

before_script:
  - bundle install
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare

script:
  - bundle exec rake

notifications:
    email: false

