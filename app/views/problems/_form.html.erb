<%= form_for @problem do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <br>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <br>

  <% guilds_array = current_user.guilds { |g| [g.name, g.id] } %>


  <%= f.submit %>

<% end %>
