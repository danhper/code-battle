<%= form_for @code do |f| %>

  <%= f.label :source %>
  <%= f.text_area :source %>

  <br>

  <% guilds_array = current_user.guilds { |g| [g.name, g.id] } %>

  <%= f.select :guild_id, guilds_array %>
<% end %>
